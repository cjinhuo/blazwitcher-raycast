# 原生单栏版本验证记录

日期：2026-09-16。本机 Raycast 2.4.1.0、SDK 2.4.1。以下只记录当前原生单栏版本；旧双栏和独立浮窗的验收不算当前版本完成。

## 自动化和构建

- `npm run check`：类型、28 项测试、ESLint、Prettier、Raycast 构建通过。
- `openspec validate restore-raycast-single-list --strict --no-interactive`：通过。
- 回归覆盖 SQLite 锁冲突稳定快照和 WAL、配置发现、书签错误隔离、真实标签 ID、URL 参数化和错误提示。
- 匹配覆盖中文、全拼、首字母、混合、跨字段、Unicode 原始范围、空查询、排序和取消。
- 数据服务覆盖单来源失败、配置加载竞态、关闭无痕、50 条分页、过期查询和过期记录拒绝。
- 20,000 条长 URL 样本在 64 MB old-space 上限内完成连续拼音搜索。

## 真实 Raycast / Chrome

| 场景             | 观察结果                                                                                                |
| ---------------- | ------------------------------------------------------------------------------------------------------- |
| 原生书签入口     | 成功打开单栏，读取 489 条书签，无右侧详情                                                               |
| 书签全拼         | `beiwang` 首条为公开书签 `git-flow 备忘清单`                                                            |
| 统一入口         | 读取 20,507 条记录，其中历史上限 20,000；显示来源下拉框、标题、灰色 URL、来源及底部操作                 |
| 混合查询         | `git beiwang` 同时找到目标书签和历史记录，条目保留来源区分                                              |
| 复制地址         | `⌘C` 显示“已复制地址”成功提示；实现使用校验过版本的原始 URL                                             |
| 打开书签         | Enter 后 Raycast 关闭；Chrome 实际显示 `git-flow 备忘清单`，地址为预期公开网页                          |
| 原生过滤高亮实验 | `filtering=true` 时输入“周报”，标题和 URL 均未着色；输入 `zhoubao` 通过 keywords 匹配后同样未给汉字着色 |
| 独立应用清理     | 本次创建的应用进程已退出，安装产物及代码已移动至 `.local/rejected-desktop/`，四个入口不再引用它         |

初期出现旧无界面入口的 Hooks 错误；更新 SDK、四个 view 清单并重新构建后实际原生列表已正常显示。不把这一临时加载故障作为高亮限制的依据；高亮结论同时有宿主源码和最小列表实际截图支持。

## 未完成与未实测

- **行内彩色高亮未实现**：内置 Clipboard History 的 HighlightRoot 没有公开扩展接口。详见 [调查](Raycast列表高亮调查.md)。
- 标题与 URL 使用原生 title/subtitle 同行显示，自定义双行布局未实现。
- 这轮未完整人工覆盖来源下拉切换、深色主题、长列表滚动到末页、输入法组合输入、权限拒绝/恢复及标签跨窗口移动；相关核心逻辑有单元回归，不能据此声称这些界面场景全部通过。
- 没有修改 Raycast 应用资源或私有数据库，没有提交、推送或发布。
